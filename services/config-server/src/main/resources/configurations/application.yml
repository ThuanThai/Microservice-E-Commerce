eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
name:
  value: microservices-config-server
spring:
  cloud:
    config:
      override-system-properties: false
